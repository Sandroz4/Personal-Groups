const names = ['gio', 'saba', 'luka']

// console.log(names.map(name))

// names.map((name) => console.log(name))


// names.map(     function(name, index) {
//     console.log(name, index)
// }     )


names.map( name => console.log(name))

names.map( function(name) {
    console.log(name)
})